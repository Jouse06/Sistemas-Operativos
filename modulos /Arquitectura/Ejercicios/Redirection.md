# Write a Bash script that redirects the output of a command to a file.
```bash
#!/bin/bash

# Command to redirect output
command_to_redirect > output.txt    
```

## Write a terminal command to redirect the output of `ls` to a file called `contents.txt`.
```bash
ls > contents.txt
```
